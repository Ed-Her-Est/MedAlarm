<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="#2FA59E">

    <!-- Imagen decorativa centrada -->
    <ImageView
        android:id="@+id/imageView3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="20dp"
        app:srcCompat="@drawable/icons8_sobre_externo_de_medicamento_disponible_y_medicamentos_sin_receta_color_tal_revivo_100" />

    <!-- Título de bienvenida -->
    <TextView
        android:id="@+id/tv_welcome_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:text="¡Cuida tu Salud!"
        android:textColor="#FFFFFF"
        android:textSize="20sp" />

    <!-- CardView con contenido -->
    <androidx.cardview.widget.CardView
        android:layout_width="344dp"
        android:layout_height="542dp"
        android:layout_marginTop="20dp"
        app:cardBackgroundColor="#FFFFFF"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="481dp"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Imagen de perfil en contenedor ovalado -->
            <androidx.cardview.widget.CardView
                android:layout_width="150dp"
                android:layout_height="150dp"
                android:layout_marginBottom="16dp"
                app:cardCornerRadius="75dp"
                app:cardElevation="4dp">

                <ImageView
                    android:id="@+id/imageView4"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    app:srcCompat="@drawable/pngwing" />
            </androidx.cardview.widget.CardView>

            <!-- Nombre del usuario registrado -->
            <TextView
                android:id="@+id/tv_registered_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="10dp"
                android:text="Nombre del Usuario"
                android:textColor="#000000"
                android:textSize="18sp" />

            <!-- Leyenda de tratamientos -->
            <TextView
                android:id="@+id/tv_no_treatment"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="20dp"
                android:text="Ningún Tratamiento Activo"
                android:textColor="#FF0000"
                android:textSize="16sp" />

            <!-- Botón para añadir tratamiento -->
            <Button
                android:id="@+id/btn_add_treatment"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:backgroundTint="#2FA59E"
                android:padding="10dp"
                android:text="Añadir tratamiento"
                android:textColor="#FFFFFF" />

        </LinearLayout>
    </androidx.cardview.widget.CardView>

</LinearLayout>
